@model IEnumerable<GestaoEstudantil.Models.Disciplina>

@{
    ViewBag.Title = "Index";
}

<div class="row">
    <div class="col-9">
        <h2>@ViewBag.pageContex</h2>
    </div>
    <div class="col-3">
        @Html.ActionLink("Adicionar Disciplina", "Create", "Disciplinas", new { @class = "btn btn-primary" })
    </div>
</div>

<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.Nome)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.CodigoDisciplina)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.ProfessorId)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.Nome)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.CodigoDisciplina)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.Professor.Nome)
        </td>
        <td>
            <div class="btn-group" role="group" aria-label="Basic outlined example">
                @Html.ActionLink("Edit", "Edit", new { id = item.Id }, new { @class = "btn btn-secondary" })
                @Html.ActionLink("Detalhes", "Details", new { id = item.Id }, new { @class = "btn" })
                @Html.ActionLink("Delete", "Delete", new { id = item.Id }, new { @class = "btn btn-danger" })
            </div>
        </td>
    </tr>
}

</table>
